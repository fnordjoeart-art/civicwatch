---
---
<!DOCTYPE html>
<html lang="it">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Civic Watch – Segnalazioni cittadino & Comune</title>
  <meta name="description" content="Piattaforma per segnalazioni dei cittadini con geolocalizzazione, foto, workflow, notifiche e integrazioni." />
  <meta name="theme-color" content="#6570F5" />
  <link rel="icon" href="/logo.jpg">
  <style>
    :root{
      --bg:#0b0d12; --panel:#111520; --muted:#8ea0c6; --text:#eaf0ff;
      --brand:#6570F5; --brand-2:#C0C4EF; --accent:#FFD34E;
      --header-alpha:.7; --shadow:0 10px 30px rgba(0,0,0,.35);
    }
    @media (prefers-color-scheme: light){
      :root{ --bg:#f7f9fc; --panel:#ffffff; --text:#0f172a; --muted:#475569; --shadow:0 10px 30px rgba(2,6,23,.08) }
    }
    html,body{height:100%}
    body{margin:0;background:var(--bg);color:var(--text);font:16px/1.6 system-ui,-apple-system,Segoe UI,Roboto,Inter,Helvetica,Arial,sans-serif}
    a{color:inherit;text-decoration:none} img{max-width:100%;display:block}
    .container{max-width:1200px;margin:0 auto;padding:0 20px}
    .btn{display:inline-flex;align-items:center;gap:.6rem;border:1px solid transparent;background:var(--brand);color:#fff;padding:.8rem 1.1rem;border-radius:14px;font-weight:700;box-shadow:var(--shadow);transition:.25s}
    .btn.secondary{background:transparent;border-color:rgba(255,255,255,.16);color:var(--text)}
    header{position:sticky;top:0;z-index:50;background:rgba(17,21,32,var(--header-alpha));backdrop-filter:saturate(1.2) blur(10px);border-bottom:1px solid rgba(255,255,255,.06)}
    .nav{display:flex;align-items:center;justify-content:space-between;padding:14px 0}
    .brand{display:flex;align-items:center;gap:.7rem;font-weight:800}
    .brand img{width:28px;height:28px;border-radius:6px;object-fit:cover}
    nav ul{display:flex;gap:1rem;list-style:none;margin:0;padding:0} nav a{opacity:.9;font-weight:600}
    .menu-btn{display:none}
    @media (max-width: 900px){
      nav ul{display:none}.menu-btn{display:flex}
      #mobileMenu{display:none;position:absolute;left:0;right:0;top:100%;background:var(--panel);border-bottom:1px solid rgba(255,255,255,.06)}
      #mobileMenu a{display:block;padding:14px 20px;border-top:1px solid rgba(255,255,255,.06)}
    }
    .hero{position:relative;padding:72px 0 40px;background:
      radial-gradient(1200px 600px at 70% -10%, #6570F540, transparent 60%),
      radial-gradient(600px 300px  at 0%  10%, #C0C4EF40, transparent 60%)}
    .hero h1{font-size:clamp(2rem,3.5vw + .8rem,3.6rem);line-height:1.08;margin:.2rem 0 .7rem}
    .hero p{max-width:760px;color:var(--muted)}
    .badges{display:flex;flex-wrap:wrap;gap:.6rem;margin:1rem 0}
    .badge{display:inline-flex;align-items:center;gap:.5rem;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.12);padding:.4rem .6rem;border-radius:12px}
    .actions{display:flex;gap:.8rem;flex-wrap:wrap;margin:1.1rem 0 0}
    .grid{display:grid;gap:16px}
    @media(min-width:720px){.grid.cols-3{grid-template-columns:repeat(3,1fr)}.grid.cols-2{grid-template-columns:repeat(2,1fr)}}
    .card{background:var(--panel);border:1px solid rgba(255,255,255,.08);border-radius:18px;padding:18px;box-shadow:var(--shadow)}
    section{padding:64px 0}.section-title{margin:0 0 12px;font-size:clamp(1.6rem,2vw + .4rem,2.2rem)}.section-desc{color:var(--muted);max-width:800px}
    /* Pannelli */
    .style-fab{position:fixed;right:16px;bottom:76px;z-index:60}
    .style-panel{position:fixed;right:16px;bottom:130px;z-index:70;width:min(340px,calc(100% - 32px));background:var(--panel);border:1px solid rgba(255,255,255,.12);border-radius:14px;box-shadow:var(--shadow);padding:12px;display:none}
    .style-panel label{display:flex;align-items:center;justify-content:space-between;gap:10px;margin:8px 0}
    .style-panel input[type="color"]{width:36px;height:36px;border:none;background:transparent;padding:0}
    .style-panel input[type="range"]{width:140px}
    .admin-fab{position:fixed;right:16px;bottom:136px;z-index:60}
    .admin-panel{position:fixed;right:16px;bottom:200px;z-index:80;width:min(420px,calc(100% - 32px));max-height:70vh;overflow:auto;background:var(--panel);border:1px solid rgba(255,255,255,.12);border-radius:14px;box-shadow:var(--shadow);padding:14px;display:none}
    .admin-panel label{display:block;margin:8px 0 4px;font-weight:600}
    .admin-row{display:grid;grid-template-columns:1fr 1fr;gap:10px}
    .export-area{width:100%;height:220px;border-radius:10px;border:1px solid rgba(255,255,255,.2);padding:.7rem;background:rgba(255,255,255,.03);color:var(--text)}
  </style>
</head>
<body>
  <header>
    <div class="container nav" role="navigation" aria-label="Primaria">
      <a class="brand" href="#home" aria-label="Civic Watch home">
        <img src="/logo.jpg" alt="Logo"><span>Civic&nbsp;Watch</span>
      </a>
      <nav>
        <button class="btn secondary menu-btn" id="menuBtn" aria-expanded="false" aria-controls="mobileMenu">Menu</button>
        <ul>
          <li><a href="#funzioni">Funzioni</a></li>
          <li><a href="#come-funziona">Come funziona</a></li>
          <li><a href="#per-il-comune">Per il Comune</a></li>
          <li><a href="#per-il-cittadino">Per il Cittadino</a></li>
          <li><a href="#prezzi">Prezzi</a></li>
          <li><a href="#faq">FAQ</a></li>
          <li><a class="btn" href="#contatti">Richiedi demo</a></li>
        </ul>
      </nav>
    </div>
    <div id="mobileMenu" class="container">
      <a href="#funzioni">Funzioni</a><a href="#come-funziona">Come funziona</a>
      <a href="#per-il-comune">Per il Comune</a><a href="#per-il-cittadino">Per il Cittadino</a>
      <a href="#prezzi">Prezzi</a><a href="#faq">FAQ</a><a class="btn" href="#contatti">Richiedi demo</a>
    </div>
  </header>

  <main id="home" class="hero">
    <div class="container">
      <div class="badges" aria-label="integrazioni e conformità">
        <span class="badge">🔐 SPID/CIE</span><span class="badge">💳 pagoPA</span><span class="badge">📲 IO</span><span class="badge">🛡️ GDPR</span><span class="badge">♿ AgID</span>
      </div>
      <h1>Segnalazioni rapide, gestione trasparente, cittadini coinvolti</h1>
      <p>Una piattaforma completa per raccogliere e smistare le segnalazioni dei cittadini con geolocalizzazione, foto, workflow personalizzabili e notifiche sullo stato di avanzamento. Pensata per i <strong>Comuni</strong> e semplice per i <strong>cittadini</strong>.</p>
      <div class="actions">
        <a class="btn" href="#contatti">Richiedi una demo</a>
        <a class="btn secondary" href="#come-funziona">Guarda come funziona →</a>
      </div>
      <div class="grid cols-3" style="margin-top:24px">
        <article class="card"><h3>Segnala con 1 tap</h3><p>Foto, descrizione, categoria e posizione auto.</p></article>
        <article class="card"><h3>Smistamento smart</h3><p>Regole per assegnare al settore competente.</p></article>
        <article class="card"><h3>Stato tracciato</h3><p>Notifiche push e cronologia interventi.</p></article>
      </div>
    </div>
  </main>

  <section id="funzioni">
    <div class="container">
      <h2 class="section-title">Funzioni chiave</h2>
      <p class="section-desc">Tutto il necessario per passare dalle segnalazioni alla soluzione, con tempi e responsabilità chiare.</p>
      <div class="grid cols-3">
        <article class="card"><h3>Geolocalizzazione & foto</h3><p>Coordinate precise e immagini per una presa in carico efficace.</p></article>
        <article class="card"><h3>Workflow personalizzabili</h3><p>Stati, SLA e assegnazioni con automazioni.</p></article>
        <article class="card"><h3>Notifiche & trasparenza</h3><p>Push/mail sull’avanzamento; cruscotto pubblico opzionale.</p></article>
        <article class="card"><h3>Categorie & canali</h3><p>Catalogo personalizzabile, canale web/app, input PEC.</p></article>
        <article class="card"><h3>Dashboard & report</h3><p>Statistiche, heatmap, export XLS/CSV, open data.</p></article>
        <article class="card"><h3>Integrazioni</h3><p>SPID/CIE, pagoPA, App IO, GIS e protocolli.</p></article>
      </div>
    </div>
  </section>

  <section id="prezzi" hidden>
    <div class="container">
      <h2 class="section-title">Prezzi</h2>
      <p class="section-desc">Licenza in abbonamento con supporto e aggiornamenti. Sconti per piccoli comuni e Unioni.</p>
      <div class="grid cols-3">
        <div class="card"><h4>Start</h4><div class="price">€ 149<span class="subtle">/mese</span></div></div>
        <div class="card"><h4>Pro</h4><div class="price">€ 299<span class="subtle">/mese</span></div></div>
        <div class="card"><h4>Enterprise</h4><div class="price">Su preventivo</div></div>
      </div>
    </div>
  </section>

  <section id="faq">
    <div class="container">
      <h2 class="section-title">FAQ</h2>
      <details><summary>È conforme a GDPR e AgID?</summary><p>Sì: minimizzazione dati, DPA, cifratura in transito e a riposo, log, backup, accessibilità WCAG 2.2 AA.</p></details>
      <details><summary>Serve un’app o basta il web?</summary><p>PWA con scorciatoia home, notifiche e accesso offline leggero.</p></details>
    </div>
  </section>

  <section id="contatti">
    <div class="container">
      <h2 class="section-title">Richiedi una demo</h2>
      <form class="card" onsubmit="event.preventDefault();alert('Grazie! Ti ricontatteremo a breve.');">
        <label>Nome<br/><input required style="width:100%;padding:.8rem;border-radius:10px;border:1px solid rgba(255,255,255,.2);background:transparent;color:var(--text)"></label>
        <div style="height:10px"></div>
        <label>Email<br/><input type="email" required style="width:100%;padding:.8rem;border-radius:10px;border:1px solid rgba(255,255,255,.2);background:transparent;color:var(--text)"></label>
        <div style="height:10px"></div>
        <label>Messaggio<br/><textarea rows="4" style="width:100%;padding:.8rem;border-radius:10px;border:1px solid rgba(255,255,255,.2);background:transparent;color:var(--text)"></textarea></label>
        <div style="margin-top:12px"><button class="btn" type="submit">Invia richiesta</button></div>
      </form>
    </div>
  </section>

  <footer>
    <div class="container" style="display:flex;justify-content:space-between;gap:16px;flex-wrap:wrap">
      <div class="brand"><img src="/logo.jpg" alt="Logo"><strong>Civic&nbsp;Watch</strong></div>
      <small class="subtle">© <span id="y"></span> Civic Watch – Tutti i diritti riservati.</small>
    </div>
  </footer>

  <!-- Pannello Stile -->
  <button id="styleBtn" class="btn style-fab" aria-label="Apri pannello stile">🧩 Stile</button>
  <div id="stylePanel" class="style-panel" role="dialog" aria-modal="true" aria-labelledby="styleTitle">
    <strong id="styleTitle">Aspetto del sito</strong>
    <label>Colore brand <input id="inpBrand" type="color" value="#6570F5" /></label>
    <label>Opacità header <input id="inpHdrAlpha" type="range" min="0" max="1" step="0.05" value="0.7" /></label>
    <div style="display:flex;gap:8px;align-items:center;margin-top:8px">
      <button id="btnResetStyle" class="btn secondary" type="button">Reset</button>
      <button id="btnCloseStyle" class="btn" type="button">Chiudi</button>
    </div>
  </div>

  <!-- Pannello Admin -->
  <button id="adminBtn" class="btn admin-fab" aria-label="Apri pannello admin" style="display:none">🔒 Admin</button>
  <div id="adminPanel" class="admin-panel" role="dialog" aria-modal="true" aria-labelledby="adminTitle">
    <h3 id="adminTitle">Pannello di controllo</h3>
    <label>Logo (testo)</label><input id="admLogoText" type="text" placeholder="Civic Watch" />
    <div class="admin-row">
      <div><label>Colore brand</label><input id="admBrand" type="text" placeholder="#6570F5" /></div>
      <div><label>Opacità header</label><input id="admHdrAlpha" type="text" placeholder="0.7" /></div>
    </div>
    <label>Hero – Titolo</label><input id="admHeroTitle" type="text" placeholder="Segnalazioni rapide, gestione trasparente, cittadini coinvolti" />
    <label>Hero – Sottotitolo</label><textarea id="admHeroSub" rows="3" placeholder="Una piattaforma completa..."></textarea>
    <label>Email contatti</label><input id="admEmail" type="email" placeholder="info@example.it" />
    <label>URL logo (opzionale)</label><input id="admLogoUrl" type="text" placeholder="https://.../logo.png" />
    <div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:10px">
      <button id="admApply" class="btn" type="button">Applica</button>
      <button id="admClose" class="btn secondary" type="button">Chiudi</button>
      <button id="admExport" class="btn" type="button">Copia codice</button>
    </div>
    <div id="exportWrap" style="display:none;margin-top:10px">
      <textarea id="exportCode" class="export-area" readonly></textarea>
      <div class="subtle" style="margin-top:6px">Codice pronto da incollare.</div>
    </div>
  </div>

  <button id="themeBtn" class="btn" style="position:fixed;right:16px;bottom:16px;z-index:60" aria-label="Cambia tema">🌓 Tema</button>

  <script>
    // Menu mobile
    const mBtn=document.getElementById('menuBtn'); const mMenu=document.getElementById('mobileMenu');
    if(mBtn){ mBtn.addEventListener('click',()=>{const o=(mMenu.style.display==='block'); mMenu.style.display=o?'none':'block'; mBtn.setAttribute('aria-expanded', String(!o));}); }
    document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href'); if(id && id!=='#'){ e.preventDefault(); document.querySelector(id)?.scrollIntoView({behavior:'smooth',block:'start'}); mMenu.style.display='none'; mBtn.setAttribute('aria-expanded','false'); }}));
    document.getElementById('y').textContent=new Date().getFullYear();

    // Tema
    const tBtn=document.getElementById('themeBtn'); tBtn.addEventListener('click',()=>{const c=document.documentElement.classList; if(c.contains('theme-light')){ c.remove('theme-light'); localStorage.removeItem('theme'); } else { c.add('theme-light'); localStorage.setItem('theme','light'); }}); if(localStorage.getItem('theme')==='light') document.documentElement.classList.add('theme-light');

    // Pannello Stile
    const styleBtn=document.getElementById('styleBtn'); const stylePanel=document.getElementById('stylePanel');
    const $=(id)=>document.getElementById(id); const inputs={brand:$('inpBrand'), hdrAlpha:$('inpHdrAlpha')};
    const setVar=(k,v)=>document.documentElement.style.setProperty(k,v);
    const save=()=>{ localStorage.setItem('styleCfg', JSON.stringify({brand:inputs.brand.value, hdrAlpha:inputs.hdrAlpha.value})); };
    const load=()=>{ try{ const cfg=JSON.parse(localStorage.getItem('styleCfg')); if(!cfg) return; inputs.brand.value=cfg.brand||inputs.brand.value; inputs.hdrAlpha.value=cfg.hdrAlpha||inputs.hdrAlpha.value; apply(); }catch(e){} };
    const apply=()=>{ setVar('--brand', inputs.brand.value); setVar('--header-alpha', inputs.hdrAlpha.value); };
    styleBtn.addEventListener('click', ()=>{ stylePanel.style.display=(stylePanel.style.display==='block'?'none':'block'); });
    $('btnCloseStyle').addEventListener('click', ()=> stylePanel.style.display='none');
    $('btnResetStyle').addEventListener('click', ()=>{ localStorage.removeItem('styleCfg'); inputs.brand.value='#6570F5'; inputs.hdrAlpha.value=0.7; apply(); });
    Object.values(inputs).forEach(inp=> inp.addEventListener('input', ()=>{ apply(); save(); })); load();

    // Pannello Admin (senza password in locale; mettila quando pubblichi)
    const adminBtn=$('adminBtn'); const adminPanel=$('adminPanel');
    if(new URLSearchParams(location.search).get('admin')==='1'){ adminBtn.style.display='inline-flex'; }
    const ADMIN_PASS = "Settembre45!"; // es: 'CivicWatch123' per abilitarla
    adminBtn.addEventListener('click', ()=>{
      if(ADMIN_PASS && !sessionStorage.getItem('admOK')){
        const p = prompt('Password amministratore'); if(p!==ADMIN_PASS) return alert('Password errata'); sessionStorage.setItem('admOK','1');
      }
      adminPanel.style.display = (adminPanel.style.display==='block'?'none':'block');
      // precompila
      $('admLogoText').value=document.querySelector('.brand span')?.textContent?.trim()||'Civic Watch';
      $('admBrand').value=getComputedStyle(document.documentElement).getPropertyValue('--brand').trim()||'#6570F5';
      $('admHdrAlpha').value=getComputedStyle(document.documentElement).getPropertyValue('--header-alpha').trim()||'0.7';
      $('admHeroTitle').value=document.querySelector('.hero h1')?.textContent?.trim()||'';
      $('admHeroSub').value=document.querySelector('.hero p')?.textContent?.trim()||'';
      $('admEmail').value=document.querySelector('a[href^="mailto:"]')?.textContent?.trim()||'info@example.it';
      $('admLogoUrl').value=document.querySelector('link[rel="icon"]')?.href||'';
    });
    $('admClose').addEventListener('click', ()=> adminPanel.style.display='none');
    $('admApply').addEventListener('click', ()=>{
      const brandSpan=document.querySelector('.brand span'); if(brandSpan) brandSpan.textContent=$('admLogoText').value||'Civic Watch';
      setVar('--brand', $('admBrand').value||'#6570F5'); setVar('--header-alpha', $('admHdrAlpha').value||'0.7');
      const h1=document.querySelector('.hero h1'); if(h1) h1.textContent=$('admHeroTitle').value||h1.textContent;
      const hp=document.querySelector('.hero p'); if(hp) hp.textContent=$('admHeroSub').value||hp.textContent;
      const mail=document.querySelector('a[href^="mailto:"]'); if(mail){ mail.textContent=$('admEmail').value; mail.href='mailto:'+$('admEmail').value; }
      if($('admLogoUrl').value){ let link=document.querySelector('link[rel="icon"]'); if(!link){ link=document.createElement('link'); link.rel='icon'; document.head.appendChild(link);} link.href=$('admLogoUrl').value; }
      alert('Modifiche applicate ✅');
    });
    $('admExport').addEventListener('click', ()=>{
      const blob=new Blob([document.documentElement.outerHTML],{type:'text/html'});
      const r=new FileReader(); r.onload=()=>{ const ta=$('exportCode'); $('exportWrap').style.display='block'; ta.value=r.result; ta.select(); document.execCommand('copy'); alert('Codice copiato ✅'); }; r.readAsText(blob);
    });
  </script>
</body>
</html>
